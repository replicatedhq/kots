FROM repldev/ship-cluster-api

ADD ./package.json /src/package.json
ADD ./yarn.lock /src/yarn.lock
WORKDIR /src
ADD . /src

RUN make build

ENTRYPOINT ["node", "--no-deprecation", "/src/build/server/index.js"]

