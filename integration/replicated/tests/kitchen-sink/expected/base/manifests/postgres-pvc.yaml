kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sentry-postgresql
  labels:
    app: postgresql
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
