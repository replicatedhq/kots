apiVersion: apps/v1
kind: Deployment
metadata:
  name: test
  annotations:
    kots.io/creation-phase: "not-a-number"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
      - name: nginx
        image: nginx

