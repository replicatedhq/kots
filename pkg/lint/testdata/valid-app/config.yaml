apiVersion: kots.io/v1beta1
kind: Config
metadata:
  name: my-config
spec:
  groups:
    - name: example_settings
      title: My Example Settings
      items:
        - name: hostname
          title: Hostname
          type: text
          default: example.com

