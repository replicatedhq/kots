apiVersion: v1
kind: Pod
metadata:
  name: security-test-example-chart-test-connection
  namespace: helmns
spec:
  containers:
  - args:
    - security-test-example-chart:80
    command:
    - wget
    image: busybox
    name: wget
  restartPolicy: Never
