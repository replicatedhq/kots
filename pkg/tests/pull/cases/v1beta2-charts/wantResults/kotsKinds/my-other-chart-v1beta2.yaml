apiVersion: kots.io/v1beta2
kind: HelmChart
metadata:
  name: my-other-chart
  annotations:
    kots.io/exclude: "true"
spec:
  chart:
    name: my-other-chart
    chartVersion: 0.1.0
  releaseName: my-other-chart-release
  values:
    my-value: my-value
