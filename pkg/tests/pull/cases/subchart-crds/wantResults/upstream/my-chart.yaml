apiVersion: kots.io/v1beta1
kind: HelmChart
metadata:
  name: my-chart
  annotations:
    kots.io/exclude: "true"
spec:
  chart:
    name: my-chart
    chartVersion: 0.1.0
    releaseName: my-chart-release
  helmVersion: v3
  useHelmInstall: true
