apiVersion: kots.io/v1beta1
kind: Config
metadata:
  creationTimestamp: null
  name: my-app
spec:
  groups:
  - items:
    - default: "fallback.example.com"
      help_text: |
        Use this field to provide an alternative hostname for your Example Application installation.
      name: alt_hostname
      required: true
      title: Alternative Hostname
      type: text
    name: setup
    title: Setup
status: {}
