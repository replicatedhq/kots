apiVersion: schemas.schemahero.io/v1alpha2
kind: Table
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: image-rewrite-status
spec:
  database: kotsadm-postgres
  name: image_rewrite_status
  schema:
    postgres:
      primaryKey:
      - id
      columns:
      - name: id
        type: integer
        constraints:
          notNull: true
      - name: updated_at
        type: timestamp without time zone
      - name: current_message
        type: text
      - name: status
        type: text
