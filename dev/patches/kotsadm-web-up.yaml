spec:
  template:
    spec:
      volumes:
      - name: dev
        hostPath:
          path: __PROJECT_DIR__/web
          type: Directory
      containers:
      - name: kotsadm-web
        image: kotsadm-web-dev
        volumeMounts:
        - name: dev
          mountPath: /src # workdir in the container
        resources: ~
