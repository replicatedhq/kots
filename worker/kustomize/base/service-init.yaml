apiVersion: v1
kind: Service
metadata:
  name: init-server
  labels:
    app: ship-cluster-worker-init
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: init-server
  selector:
    app: ship-cluster-worker-init
