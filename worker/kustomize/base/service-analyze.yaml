apiVersion: v1
kind: Service
metadata:
  name: analyze-server
  labels:
    app: ship-cluster-worker-analyze
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: analyze-server
  selector:
    app: ship-cluster-worker-analyze
