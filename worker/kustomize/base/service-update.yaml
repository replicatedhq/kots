apiVersion: v1
kind: Service
metadata:
  name: update-server
  labels:
    app: kotsadm-worker-update
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: update-server
  selector:
    app: kotsadm-worker-update
