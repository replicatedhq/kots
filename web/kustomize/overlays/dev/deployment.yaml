apiVersion: apps/v1
kind: Deployment
metadata:
  name: kotsadm-web
spec:
  template:
    spec:
      imagePullSecrets:
        - name: okteto-regcred
      containers:
        - name: kotsadm-web
          ports:
            - name: http
              containerPort: 8080