apiVersion: v1
kind: Service
metadata:
  name: ship-cluster-ui
  labels:
    app: ship-cluster-ui
spec:
  type: NodePort
  ports:
  - name: http
    port: 3000
    targetPort: http
  selector:
    app: ship-cluster-ui
