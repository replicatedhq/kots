import React, { Component } from "react";
import { withRouter } from "react-router-dom";

class ConsoleRegistry extends Component {
  render() {
    return (
      <div>
        <p>Console registry</p>
      </div>
    )
  }
}

export default withRouter(ConsoleRegistry);